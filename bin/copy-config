#!/bin/bash
# Copy config files that are present in the home directory.
# Usage: copy-config src dest

src="$1"
dest="$2"

for file in .bash* .emacs .git?* .minttyrc .screenrc; do
  if [ -f ~/$file ]; then
    cp "$src/$file" "$dest/$file"
  fi
done
